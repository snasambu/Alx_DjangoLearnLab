### Task: CRUD Operations on the Book Model

Below are the commands and outputs from the Django shell for the specified CRUD operations on the ookshelf.Book model.

### 1. Create Operation

**Command:** c:\Users\USER\Desktop\alx_be_python\bookshelf\models.py
git add .
git commit -m "Documented CRUD operations for the bookshelf app"
git push origin main
